import { VideoHandlerDirective } from './video-handler.directive';

describe('VideoHandlerDirective', () => {
  it('should create an instance', () => {
    const directive = new VideoHandlerDirective();
    expect(directive).toBeTruthy();
  });
});
